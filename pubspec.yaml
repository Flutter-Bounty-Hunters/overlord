name: overlord
description: An assortment of widgets that loom over your content.
version: 0.0.3+2
homepage: https://github.com/flutter-bounty-hunters/overlord

environment:
  sdk: '>=2.18.0 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  follow_the_leader: ^0.0.4+2
  logging: ^1.0.1

#dependency_overrides:
#  follow_the_leader:
#    git:
#      url: https://github.com/Flutter-Bounty-Hunters/follow_the_leader
#      ref: main

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  golden_toolkit: ^0.11.0

  # For loading icon font files in tests
  file: ^6.1.4
  platform: ^3.1.0

flutter:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
